{
  "name": "orbital-commerce",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "dev:gateway": "node --env-file-if-exists=.env.local server.mjs",
    "serve:static": "node --env-file-if-exists=.env.local server.mjs"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
